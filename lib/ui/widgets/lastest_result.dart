import 'package:flutter/material.dart';

class LatestResult extends StatelessWidget {
  const LatestResult({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Latest Activity");
  }
}
